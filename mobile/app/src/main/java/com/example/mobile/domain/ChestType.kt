package com.example.mobile.domain

/**
 * Enum representing different chest types in the game.
 * Each chest type has different reward potentials and rarity.
 */
enum class ChestType {
    NORMAL,
    RARE,
    EPIC,
    LEGENDARY
}