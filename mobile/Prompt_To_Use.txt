Complete task 7 (GLOBAL STREAK EVENT) from TODO.md.

## CRITICAL RULES (NON-NEGOTIABLE)

- Do NOT invent APIs, classes, composables, imports, or packages.
- Do NOT assume a symbol exists unless it is shown in provided code or official AndroidX APIs.
- The code must compile exactly as written.

## IMPORT AND PACKAGE CONSISTENCY RULE

Before creating or modifying any file:

1. Always first inspect the provided codebase context for existing implementations:
   Entity, Repository, Event, UseCase, Model, DTO, State, Action, ViewModel, Mapper, Manager, or Service.

2. Prefer reusing existing packages, files, and imports over creating new ones.

3. Never guess package names or file locations.

4. Match existing project structure:
   - data.local.entities
   - domain.repository
   - presentation.ui.*
   (only if already used in the project)

5. If multiple versions of a class exist:
   - Use the one already used in the project
   - Do NOT create duplicates

## BUILD VERIFICATION RULE

A task is NOT complete unless:

- Code compiles successfully
- Imports and packages are correct
- No duplicate or conflicting types exist

If any of these fail → fix before proceeding.

## FINAL REPORT REQUIREMENTS

After completion, provide:

- Files changed
- Files created
- Documentation updated
- TODO.md progress update
- Remaining issues (if any)